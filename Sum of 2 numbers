// Online C compiler to run C program online
#include <stdio.h>

int main() {
    // Write C code here
    int a, b, c;
    printf("\nenter two numbers");
    scanf("%d%d",&a,&b);
    c=a+b;
    printf ("\nsum of a&b is %d",c);
    
    return 0;
}
