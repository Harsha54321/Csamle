// Online C compiler to run C program online
#include <stdio.h>

int main() {
    // Write C code here
    int x, y;
    printf("\n Enter two different numbers to test the biggest number among two numbers");
    scanf("%d%d",&x,&y);
    printf ("\n x=%d,y=%d",x,y);
    x>y? printf ("\n %d is biggest number",x):printf ("\n %d is biggest number",y);
    
    return 0;
}
